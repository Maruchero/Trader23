# Trader23

## Running

1. Install dependencies
    ```
    npm install
    ```
1. You have two options:
    - Run in development
      ```
      npm start
      ```
    - Build the app
      ```
      npm run build
      ```
